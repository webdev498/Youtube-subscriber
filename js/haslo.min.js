function haselko()
{
	var zn_sp=document.getElementById("zn_sp").checked;
	var los=document.getElementById("loso").checked;
	var liczba_zn=document.getElementById("liczba_zn").value;
  var haslo="";
  var tablica= "qwertyuiopasdfghjklzxcvbnmMNBVCXZLKJHGFDSAPOIUYTREWQ";
  var tablica_roz="1234567890_-+=!@#$%^&*()`~`''|\ąźćńłó)";
  var dlugosc=6;
  if (zn_sp)
	{
	tablica+=tablica_roz;
	}	
	if (los)
	{
			var dlugosc= Math.floor(Math.random() * 15) +6;
	}
	if(liczba_zn>0)
	{
		var dlugosc= liczba_zn;
	}
	
  for	(i=0; i<dlugosc; i++ )
  {
	var liczba= Math.random()*tablica.length;
	var znak= tablica.charAt(liczba);
	haslo+=znak;
	}	
	document.getElementById("haslo").value=haslo;
	document.getElementById("haslo2").value=haslo;
	sila(haslo);
}

function sila (password) 
{
  var desc = new Array();
        desc[0] = "Bardzo słabe";
        desc[1] = "Słabe";
        desc[2] = "Trudniejsze";
        desc[3] = "Trudne";
        desc[4] = "Mocne";
        desc[5] = "Bardzo mocne";
  var col =new Array();
	col[0]="#FF7348";
	col[1]="#B93D00";
	col[2]="#8EFF8E";
	col[3]="#49DC00";
	col[4]="#3DB900";
	col[5]="#257300";
        var score   = 0;

        if (password.length > 4) score++;
     
        if ( ( password.match(/[a-z]/) ) && ( password.match(/[A-Z]/) ) ) score++;

        if (password.match(/\d+/)) score++;

        if ( password.match(/.[!,@,#,$,%,^,&,*,?,_,~,-,(,)]/) ) score++;

        if (password.length > 8) score++;
         document.getElementById("wyp").style.background = col[score];
			var w;
			if (score<3)
			{
				w=score*10;
			}
			else
			{
				w=(score+5)*10;
			}
	
			w=w+"%";
			
			document.getElementById("wyp").style.width = w;
			document.getElementById("tek").innerHTML = desc[score]
}